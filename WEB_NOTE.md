# 网页文件说明

完整可运行的宫崎骏风格小册子网页（`index.html` / `china-tech-biotech-watch.html`）已在对话中生成并提供下载。

由于单文件体积较大，当前仓库优先备份了：

1. **结构化数据** `data/2026-09-06.json`（完整，可直接用于后续程序读取）
2. **README** 说明文档

你可以把下载到的 `china-tech-biotech-watch.html` 重命名为 `index.html` 后直接提交到本仓库根目录，即可完成网页与数据的完整备份。

后续每日更新流程建议：
1. 新增 `data/YYYY-MM-DD.json`
2. 更新网页内容或改为动态读取 JSON
3. 提交到本仓库

仓库地址：https://github.com/sina-west4angle/gork-china-stock-watch
